import 'package:flutter/material.dart';

class hadeth_tap extends StatelessWidget {
  const hadeth_tap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}