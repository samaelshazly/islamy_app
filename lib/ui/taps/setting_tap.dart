import 'package:flutter/material.dart';

class setting_tap extends StatelessWidget {
  const setting_tap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
